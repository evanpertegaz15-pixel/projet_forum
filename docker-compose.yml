services:

  app:
    build: .
    container_name: forum_app
    ports:
      - "8080:8080"
    volumes:
      - db_data:/app/data           # persists forum.db
    environment:
      - DB_PATH=/app/data/forum.db
    restart: unless-stopped

volumes:
  db_data: